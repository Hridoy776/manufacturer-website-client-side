import React from 'react';

const Loading = () => {
    return (
        <div className='flex justify-center items-center'>
            <div>loading</div>
        </div>
    );
};

export default Loading;